<%= form_for(@inactive_week) do |f| %>
  <% if @inactive_week.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inactive_week.errors.count, "error") %> prohibited this inactive_week from being saved:</h2>

      <ul>
      <% @inactive_week.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start %><br>
    <%= f.date_select :start %>
  </div>
  <div class="field">
    <%= f.label :end %><br>
    <%= f.date_select :end %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
